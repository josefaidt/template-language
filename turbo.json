{
  "$schema": "https://turborepo.org/schema.json",
  "globalDependencies": [".env", "$DEBUG"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["build/**"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    }
  }
}
